//crie uma "const" com seu nome e valor, depois uma estrutura "if que verifica se o seu nome é o que está na constante"
//se estiver, emita uma mensagem de saudacão com "console.log"

const nome = "lucas";

if (nome == "lucas") {
    console.log(`Saudacões ${nome}!`);
} else if (nome != "lucas"){
    console.log(`vc n é o ${nome}`);
}