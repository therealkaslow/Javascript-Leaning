// estrutura condicional "if" para verificar se uma condicão é verdadeira, executar um bloco de código, se não, executar o outro bloco de código.
let velocidadeBola = 100;

if (velocidadeBola > 80) {
    console.log('muito rápida');
} else {
    console.log('velocidade moderada');
}